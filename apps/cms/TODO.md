Bug Fixes:
- Currently onboarding fields that are marked optional are still required
- If a rotation isn't published, can't order signature meals. People should be able to order signature meals and it will be grouped for the next week even without "weekly meals"
- Make the profile settings page look more like a proper profile page rather than a form
